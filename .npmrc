# Save new dependencies exactly, never with ^: the lookup reads files inside
# @pnp/cli-microsoft365 (allCommandsFull.json, docs/**, dist/utils/md.js) that are
# implementation detail, not public API. `npm run contract` asserts them.
save-exact=true
